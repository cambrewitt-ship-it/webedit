{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "ignoreCommand": "git diff HEAD~1 HEAD --name-only | grep -qv '^data/usage'"
}
